name: SWA - HandyFix Web

on:
  push:
    branches: [main]
    paths:
      - "apps/handyfix-web/**"
      - "packages/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/**"
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [main]

concurrency:
  group: swa-web-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_and_deploy:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      # IMPORTANT: install from repo root so workspaces work
      - name: Install (root)
        run: npm ci

      - name: Build api-client
        run: npm -w packages/api-client run build

      - name: Build web
        run: npm -w apps/handyfix-web run build
        env:
          VITE_API_BASE_URL: "https://rg-handyfix-prod-bngheqdafje9ghhb.westus2-01.azurewebsites.net"

      - name: Deploy (Web)
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_YELLOW_WATER_05F581E1E }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"

          # Upload already-built artifacts (skip Oryx)
          app_location: "apps/handyfix-web/dist"
          output_location: ""
          api_location: ""

          skip_app_build: true
          skip_api_build: true

  close_pr:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Close PR (Web)
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_YELLOW_WATER_05F581E1E }}
          action: "close"
